# Sample blue brief

Generated by Tenaille / CVBASTION (sample). Tagline: Two faces. One host.

The text below matches `python -m tenaille assess --mode blue` for `examples/lab-web-01.json`. Same findings as the red brief. Opposite job.

## Leadership read

Two hypotheses, one inventory. Neither was shown on a live socket beyond the version and the config field written into the fixture. The lab hour is patch level and agent-forwarding policy. This is not an active-compromise briefing.

## Remediation table

| Pri | CVE | Paired red path | Lab meaning |
| --- | --- | --- | --- |
| P2 | CVE-2024-6387 | Path A | Version match on a lab fixture. Catalog weight from the frozen KEV flag. Not P1. |
| P3 | CVE-2023-38408 | Path B | Config hygiene. Agent forwarding is recorded on. No forwarded session is recorded. |

P1 is reserved for an internet-facing host with catalog weight. These fixtures cannot reach it. Raw CVSS cannot put them there. CVE-2023-38408's base score of 9.8 is why the table exists: the number is not the priority.

## Controls

**CVE-2024-6387.** Install the distribution's OpenSSH security update and record the new version. Vendor notes mention `LoginGraceTime 0` as a temporary mitigation with a denial-of-service tradeoff (`MaxStartups` exhaustion). The sample does not change a host. Detection is package drift plus a burst of authentication failures inside the grace window, read with the vendor note in hand.

**CVE-2023-38408.** Set `AllowAgentForwarding` to `no` unless a named workflow needs it. If the version match is still true after that, move to a build at or after 9.3p2 or attach a vendor advisory for this package revision. Detection is a config change on `AllowAgentForwarding` or a client `ForwardAgent` setting.

## What done means

Re-collect the inventory. Path A falls out of 8.5p1–9.7p1 or gains a named advisory. Path B has agent forwarding off, or an exception with a name and a purpose. The next brief still must not say the old image was compromised.
