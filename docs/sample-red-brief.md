# Sample red brief

Generated by Tenaille / CVINFERNO (sample). Tagline: Two faces. One host.

The text below is the teaching copy of `python -m tenaille assess --inventory examples/lab-web-01.json --mode red`. The command is the source of truth if a sentence here drifts. Both must keep the ceiling at `version_match` and must not claim the host was compromised.

Host `lab-web-01` is a synthetic Ubuntu 22.04 fixture at `203.0.113.10` (TEST-NET-3). Recorded sshd is `OpenSSH_8.9p1`. `AllowAgentForwarding` is `yes`. Exposure is `lab-only`.

## Path A — CVE-2024-6387

Upstream range in the fixture: 8.5p1 through 9.7p1. `8.9p1` sits inside it, so the version comparison matches.

- **Easy.** The evidence is the recorded version. Why it matters: public write-ups describe a high-complexity race in sshd signal handling. The brief does not say how to trigger it.
- **Medium.** No second banner, no reachability check, no changelog. CVSS 8.1 does not raise the ceiling. The fixture's KEV flag makes the lab priority P2, not an incident.
- **Hard.** A distro backport can fix `1:8.9p1-3ubuntu0.1` without moving the upstream number. That was not checked. LoginGraceTime is not in the inventory.

Not claimed: reachability, an unpatched build, or compromise.

## Path B — CVE-2023-38408

Post-foothold and config-driven. The public issue applies to OpenSSH before 9.3p2 when an agent is forwarded to a system it should not trust. `8.9p1` is before that line, and the fixture records agent forwarding as enabled. No forwarded session is in the inventory.

Context priority is P3. The NVD base score is 9.8. The sample does not let that number outrank the missing precondition and the lab-only exposure.

Ceiling for Path B is still `version_match`. Recording the config flag is not active verification.

## Difficulty is not a procedure

Easy, Medium, and Hard change how much uncertainty the paragraph admits. They do not add steps.
